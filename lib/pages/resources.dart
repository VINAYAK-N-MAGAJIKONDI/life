import 'package:flutter/material.dart';

class resources extends StatelessWidget {
  const resources({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Further Resources'),
      ),
    );
  }
}
