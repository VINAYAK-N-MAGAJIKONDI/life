import 'package:flutter/material.dart';

class appuses extends StatelessWidget {
  const appuses({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('App and Its Uses'),
      ),
    );
  }
}
