import 'package:flutter/material.dart';

class plastic extends StatelessWidget {
  const plastic({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Plastic Pollution and Its Impact'),
      ),
    );
  }
}
